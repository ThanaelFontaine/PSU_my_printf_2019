/*
** EPITECH PROJECT, 2019
** greatest
** File description:
** returns the greatest amongst the two arguments
*/

int my_greatest(int a, int b)
{
    if (a < b) {
        return (b);
    } else if (a == b) {
        return (a);
    } else {
        return (a);
    }
}
