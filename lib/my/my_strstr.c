/*
** EPITECH PROJECT, 2019
** my_strstr
** File description:
** return pointer
*/
#include <stdlib.h>

char *my_strstr(char *str, char const to_find)
{}
/*    char *tof = to_find;
    char *str2 = str;

    while (*str != '\0') {
        while (*str == *to_find) {
            to_find += 1;
            if (*to_find == '\0') {
                return (str2);
            }
            str++;
            if (*str == '\0') {
                return (NULL);
            }
        }
        to_find = tof;
        str += 1;
        str2 = str;
    }
    return (NULL);
}*/
