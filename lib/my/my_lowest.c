/*
** EPITECH PROJECT, 2019
** lowest
** File description:
** returns the lowest amongst the two arguments
*/

int my_lowest(int a, int b)
{
    if (a < b) {
        return (a);
    } else if (a == b) {
        return (a);
    } else {
        return (b);
    }
}
